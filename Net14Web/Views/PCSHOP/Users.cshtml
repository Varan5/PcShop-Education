@using Net14Web.Models.PcShop
@model List<UserViewModel>

@{
    Layout = "_LayoutPcShop";
}

<div>
    <table>
        <tr><th>Id</th><th>Логин</th><th>Имя</th></tr>
        @foreach (var User in Model)
        {
            <tr><td>@User.Id</td><td>@User.Login</td><td>@User.FirstName</td></tr>
        }
    </table>
</div>